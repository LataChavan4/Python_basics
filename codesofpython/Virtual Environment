# You can create parallel python environment using 
pip install virtualenv

#name the environment
virtualenv myproject(you can choose any name)

#once installation is done you need to activate this environment by using 
virtualenv_name/bin/activate (for linux/MacOS)
.\virtualenv_name\scripts\activate.ps1 (for windows)

To deactivate it type
Deactivate
